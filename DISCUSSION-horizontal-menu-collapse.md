# 横向菜单超出部分折叠显示讨论

## 当前实现

目前顶部横向菜单（`.custom-top-menu-container`）的处理方式：
- **实现方式**：使用 `overflow-x: auto` 允许横向滚动
- **优点**：
  - 所有菜单项都可见，用户可以通过滚动查看所有菜单
  - 实现简单，无需额外逻辑
  - 用户体验直观
- **缺点**：
  - 当菜单项较多时，需要滚动才能看到所有菜单
  - 可能不够美观，滚动条占用空间
  - 用户可能不知道有更多菜单项

## 折叠显示方案

### 方案一：自动折叠超出部分（推荐）
- **实现方式**：
  - 检测菜单项总宽度是否超出容器宽度
  - 超出时，将超出部分折叠到"更多"菜单中
  - 显示一个"更多"或"..."按钮，点击后显示下拉菜单
- **优点**：
  - 界面更整洁，不会出现滚动条
  - 所有菜单项仍然可以访问
  - 符合常见的UI设计模式
- **缺点**：
  - 需要额外的JavaScript逻辑
  - 需要处理响应式布局（窗口大小变化时重新计算）

### 方案二：响应式折叠
- **实现方式**：
  - 根据屏幕宽度动态调整显示的菜单项数量
  - 超出部分自动折叠到"更多"菜单
  - 窗口大小变化时自动重新计算
- **优点**：
  - 适配不同屏幕尺寸
  - 用户体验更好
- **缺点**：
  - 实现复杂度较高
  - 需要监听窗口resize事件

### 方案三：保持当前滚动方式
- **实现方式**：保持现状，使用横向滚动
- **优点**：
  - 实现简单
  - 无需额外代码
- **缺点**：
  - 用户体验可能不够好

## 建议

**推荐采用方案一（自动折叠超出部分）**，原因：
1. 提供更好的用户体验
2. 界面更整洁美观
3. 符合常见的UI设计模式
4. 实现复杂度适中

## 实现细节

### 需要实现的功能：
1. 检测菜单项总宽度
2. 计算哪些菜单项需要折叠
3. 创建"更多"菜单按钮
4. 实现下拉菜单显示折叠的菜单项
5. 处理窗口大小变化时的重新计算

### 技术要点：
- 使用 `ResizeObserver` 或 `window.resize` 监听容器大小变化
- 动态计算每个菜单项的宽度
- 使用下拉菜单组件（可以复用现有的菜单样式）

## 实现状态

✅ **已实现方案一（自动折叠超出部分）**

### 实现的功能：
1. ✅ 自动检测菜单项总宽度是否超出容器宽度
2. ✅ 超出时自动将超出部分折叠到"更多"菜单中
3. ✅ 显示"更多"按钮，点击后显示下拉菜单
4. ✅ 响应窗口大小变化，自动重新计算
5. ✅ 下拉菜单样式美化

### 技术实现：
- 使用 `ResizeObserver` 和 `window.resize` 监听容器大小变化
- 动态计算每个菜单项的宽度
- 使用下拉菜单显示折叠的菜单项
- 保持原有的点击事件功能

### 代码位置：
- `content.js`: `initHorizontalMenuCollapse()` 和 `applyMenuCollapse()` 函数
- `styles.css`: `.custom-more-menu` 和 `.custom-more-dropdown` 样式

## 待决定事项

- [x] 是否采用折叠显示方案 - **已采用方案一**
- [x] 折叠菜单的显示方式 - **已实现下拉菜单**
- [ ] 是否需要用户可配置（在popup中添加开关）- **可选功能**
- [ ] 是否需要添加配置选项让用户选择滚动或折叠模式
