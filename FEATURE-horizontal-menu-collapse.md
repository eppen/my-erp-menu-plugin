# 横向菜单折叠功能说明

## 功能概述

当顶部横向菜单项过多，超出屏幕宽度时，自动将超出部分折叠到"更多"菜单中，提供更好的用户体验。

## 功能特点

1. **自动检测**：自动检测菜单项总宽度是否超出容器宽度
2. **智能折叠**：超出部分自动折叠到"更多"菜单
3. **响应式**：窗口大小变化时自动重新计算和调整
4. **保持功能**：折叠的菜单项保持原有的点击功能

## 使用方法

功能默认启用，无需额外配置。当菜单项超出屏幕宽度时，会自动显示"更多"按钮。

### 操作方式：
1. 点击"更多"按钮，显示下拉菜单
2. 在下拉菜单中选择需要的菜单项
3. 点击外部区域或再次点击"更多"按钮关闭下拉菜单

## 技术实现

### 核心函数：
- `initHorizontalMenuCollapse()`: 初始化折叠功能
- `applyMenuCollapse()`: 应用折叠逻辑

### 样式类：
- `.custom-more-menu`: "更多"按钮样式
- `.custom-more-dropdown`: 下拉菜单容器样式
- `.custom-dropdown-item`: 下拉菜单项样式

## 配置选项

可以通过 `chrome.storage.sync` 中的 `horizontalMenuCollapseEnabled` 选项控制是否启用折叠功能：
- `true`（默认）：启用折叠功能
- `false`：禁用折叠功能，使用横向滚动

## 注意事项

1. 折叠功能会在菜单项渲染完成后自动执行
2. 窗口大小变化时会自动重新计算
3. 折叠的菜单项会保持原有的激活状态和点击功能
